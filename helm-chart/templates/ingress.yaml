apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: "{{ .Values.ingress.name }}"
spec:
    ingressClassName: nginx
    rules:
        - host: "{{ .Values.ingress.host }}"
          http:
              paths:
                  - path: "{{ .Values.roleManager.api.ingress.path }}"
                    pathType: "{{ .Values.roleManager.api.ingress.pathType }}"
                    backend:
                        service:
                            name: "{{ .Values.roleManager.api.service.name }}"
                            port:
                                number: {{ .Values.roleManager.api.servicePort }}
